# SRH Characterization Reports

This directory contains reports generated by the
SRH Hardware Characterization component.

Reports are specific to the host on which the characterization is
executed and are therefore not committed to the repository.

Generate a report with:

    python3 srh/characterize.py

The generated JSON contains the hardware and runtime fingerprint used
by subsequent SRH benchmarking and profile recommendation components.
